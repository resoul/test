# Реестр найденных дефектов

Каждый найденный дефект — строка здесь в момент обнаружения, до исправления: компонент, что
было не так, как найден, статус. Исправление дописывает коммит; неисправленное остаётся
**открыт** с тем, где закрыть. Порядок — по дате обнаружения.

Нумерация общая с реестром Trellis, где Espalier начинался (папка `v22/`, с 2026-09-26 — корень репозитория
[resoul/test](https://github.com/resoul/test/blob/trellis-final/docs/defects.md)): #1–#106 — дефекты
Trellis, в том числе #95–#106 — расхождения старого движка с Chromium, от которых отталкивался
новый. Отсюда — дефекты Espalier с #107; новые продолжают нумерацию с #154. Хэши коммитов в
статусах — из истории resoul/test.

| # | Дата | Компонент | Дефект | Как найден | Статус |
|---|---|---|---|---|---|
| 107 | 2026-09-24 | `LayoutCore` distribute | `space-around`/`space-evenly` при отрицательном свободном месте центрировались; по CSS Box Alignment это safe center — к началу по направлению письма (для `*-reverse`/`wrap-reverse` это flex-end). | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0235`, `0210`, `0347`. | исправлен `6f7126d` |
| 108 | 2026-09-24 | `LayoutCore` makeItem | Размер, выведенный из `aspect-ratio`, считался заданным и блокировал stretch. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0089`, `0151`. | исправлен `6f7126d` |
| 109 | 2026-09-24 | `LayoutCore` проценты | Проценты разрешались от известного, но не definite размера (высота по содержимому, §9.8). | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0300`. | исправлен `6f7126d` |
| 110 | 2026-09-24 | `LayoutCore` flex base size | Базовый размер мог быть меньше padding (border-box) и зажимался собственными min/max элемента. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0196`, `0319`. | исправлен `6f7126d` |
| 111 | 2026-09-24 | `LayoutCore` intrinsic row | Собственная ширина row-контейнера считалась по flex-basis; Chromium суммирует вклады детей (width/содержимое, при grow/shrink 0 — не дальше basis), min-content при wrap — наибольший вклад. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0319`, `0347`; пробы Chromium. | исправлен `6f7126d` |
| 112 | 2026-09-24 | `LayoutCore` aspect-ratio | Не переносились min/max через пропорцию; применялся удалённый из спецификации transferred size suggestion; не было автоминимума по оси, зависящей от пропорции (CSS Sizing 4 §5.2.1). | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0114`, `0153`, `0369`, `0013`, `0379`. | исправлен `6f7126d` |
| 113 | 2026-09-24 | `LayoutCore` проценты ширины | Известная ширина не считалась definite; в CSS §9.8 ограничивает только высоты. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0132`, `0067`. | исправлен `e23d67e` |
| 114 | 2026-09-24 | `LayoutCore` makeItem | Stretch применялся при поперечном размере-проценте (не `auto`); basis брался из width после clamp max; неразрешимый процент basis не вёл себя как `content`. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0039`, `0318`, `0181`, `0123`, `0215`. | исправлен `e23d67e` |
| 115 | 2026-09-24 | `LayoutCore` размер содержимого | «Размер содержимого» игнорировал собственные размеры по обеим осям вместо измеряемой; контейнер с `aspect-ratio` без размеров не получал высоту из пропорции. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0328`. | исправлен `e23d67e` |
| 116 | 2026-09-24 | `LayoutCore` intrinsic column | Собственная ширина column-контейнера с `wrap` складывала колонки; Blink берёт самый широкий элемент. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0008`. | исправлен `e23d67e` |
| 117 | 2026-09-24 | `LayoutCore` absolute | Auto-margin при обоих отступах не забирали остаток; в RTL при переопределении игнорировался `right` вместо `left`; высота absolute считалась definite без отступов; статическая позиция при `space-around` — не центр. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `random/0038`, `0075`, `0186`, `0351`. | исправлен `e23d67e` |
| 118 | 2026-09-24 | `LayoutCore` | Хвост расхождений с Chromium: на 2026-09-24 — 37 из 400 случайных деревьев, 24 из 200 деревьев с текстом и 32 из 150 деревьев с базовой линией; все, кроме `random/0008` и `random-baseline/0069`, — сочетания `aspect-ratio` и/или `absolute`. | Случайные деревья против Chromium (`Conformance/CSSFlexbox/fixtures/random.json`, seed 2026): `reports/engine.md`. | исправлен `90631ed`, `3040bd8` (#143–#152): 912 из 912 против Chromium 152; против записанного Chromium 141 — 910 из 912, `random/0328` и `random-text/0066` Chromium 141 и 152 раскладывают по-разному; после перегенерации эталона Chromium 153 — 993 из 993 |
| 119 | 2026-09-24 | `LayoutCore` intrinsic row | Вклад элемента в собственную ширину row прижимался к flex-basis и тогда, когда basis — неразрешимый процент; Chromium прижимает только к определённой длине. | Случайные деревья с текстом (`fixtures/random-text.json`, seed 7): `random-text/0058`, заодно `random/0059`. | исправлен `367f16b` |
| 120 | 2026-09-24 | `LayoutCore` базовая линия контейнера | Базовая линия контейнера с `wrap-reverse` бралась из логически первой строки; Chromium берёт физически верхнюю (при `wrap-reverse` — последнюю). | Случайные деревья с базовой линией (`fixtures/random-baseline.json`, seed 11): `random-baseline/0047`. | исправлен `face543` |
| 121 | 2026-09-24 | `LayoutCore` `align-items: baseline` в column | В column выравнивание по базовой линии откатывалось к `start`; Chromium синтезирует базовую линию по cross-start краю, и начала элементов выравниваются по наибольшему начальному margin. | `random-baseline/0118`. | исправлен `face543` |
| 122 | 2026-09-24 | `LayoutCore` `align-items: baseline` при `wrap-reverse` | В строке `wrap-reverse` выравнивание по базовой линии откатывалось к `start`; Chromium выравнивает первые базовые линии как обычно, а группа прижимается к cross-start краю строки (к низу). | `random-baseline/0146`. | исправлен `face543` |
| 123 | 2026-09-24 | `Conformance/CSSFlexbox/generate.cjs` | Лист с `content` получал в HTML свойства flex-контейнера (`align-items`, `justify-content`, `flex-direction`, …); размер листа от них не меняется, но внутренний блок смещался — и с ним базовая линия, которой у содержимого в движке нет. | Разбор `random-baseline/0146`: n12 с `align-items: center` давал базовую линию 45 вместо 10. Наборы без базовой линии при перегенерации не изменились побайтно. | исправлен `face543` |
| 124 | 2026-09-24 | `LayoutCore` рекурсия солвера | Солвер рекурсивен по уровню вложенности и тратил ~5.8 КиБ стека на уровень (кадр `flexLayout` 3.6 КиБ, `makeItem` 2.2 КиБ): стек 8 МиБ (главный поток macOS/Linux) кончался на ~1440 уровнях, 1 МиБ (главный поток iOS) — ~175, 512 КиБ (вторичные потоки Apple, cooperative pool) — ~88. У старого движка то же свойство (#22, #45). | Бенчмарк `Benchmarks`: `deep-1500` — SIGSEGV после оптимизаций E3; кадры замерены по `$sp` в gdb. | исправлен `2b9e8aa` — бюджет стека (`LayoutContext.stackBudget`, `LayoutStackExhausted`): проход на главном потоке не падает, а уходит на поток хоста или отклоняется; итеративный солвер не делался (решение 2026-09-25); следующий предел глубины — сборка спеки, #139. Ранее — частично: `fcc33d2`: кадры уменьшены до ~1.8 КиБ на уровень (release; 512 КиБ — 150–250 уровней, 1 МиБ — 400–500; debug — ~60–80 на 1 МиБ). Фоновый расчёт `NodeHost` идёт на потоке со стеком 8 МиБ (слой нод). Открыт — первый проход и раскладка view остаются на вызывающем потоке: для любой глубины там — итеративный солвер |
| 125 | 2026-09-24 | `LayoutCore` вложенные контейнеры | Лишняя работа на каждом уровне вложенности: (1) гипотетический поперечный размер мерился и у растягиваемого элемента однострочного контейнера с известным cross — отдельный проход по поддереву при неизвестной ширине; (2) размер родителя входил в ключ кэша и у узлов без процентов и вариантов — одинаковые запросы не совпадали; (3) автоматический минимальный размер (min-content проход по поддереву) считался всегда, хотя нужен только при сжатии строки. Цепочка глубины 16: 13 прогонов на контейнер, 5 размеров на лист. | Бенчмарк `Benchmarks`: `deep-30` в 5.8 раза, `deep-1500` в 3.5, `cards-300` в 2.5 раза медленнее старого движка; счётчики `SolveStatistics`. | исправлен `fcc33d2` |
| 126 | 2026-09-24 | `LayoutCore` DSL | `LayoutSpec.apply(in:)`/`measure`, `LayoutElement.applyLayoutFrame` и тесты DSL пользовались `CGRect`/`CGSize` со Swift-расширениями CoreGraphics (`minX`, `.zero`, `CGRect(x:y:width:height:)`). На Linux их даёт swift-corelibs-foundation, на macOS (SDK 27) `import Foundation` отдаёт только C-структуру — `LayoutCore` не собирается. | Прогон `swift test` на Mac пользователем: `value of type 'CGRect' has no member 'minX'`. | исправлен `844d282` |
| 127 | 2026-09-24 | `Benchmarks` | Бенчмарк перестал собираться: пакет потребовал Flux `from: "1.3.0"`, а Trellis закреплён на Flux ровно 1.2.1 — в одном графе зависимостей версии несовместимы. | `swift build` бенчмарка после добавления `StateFlux` (`e4a9779`). | исправлен `a69ffe6` |
| 128 | 2026-09-24 | `EngineWorkTests` | Процесс `LayoutCoreTests` на macOS падает с SIGBUS: Swift Testing выполняет тесты на потоках пула задач со стеком 512 КиБ, а `workGrowsLinearlyWithNesting` раскладывает цепочку из 100 уровней в debug-сборке (~14 КиБ стека на уровень, #124). На Linux у этих потоков стек больше — не воспроизводилось. | Прогон пользователя на Mac: `exited with unexpected signal code 10`. | исправлен `d0907d7` |
| 129 | 2026-09-24 | `NodesRender` `LayerRenderer.draw` | Текст в `NodeNSView` показывался перевёрнутым по вертикали: рендерер рисовал содержимое заранее перевёрнутым, если `contentsAreFlipped()` слоя возвращал `true`. Этот флаг описывает контекст рисования в `draw(in:)`, а готовая картинка в `contents` показывается как есть. Тест `aFlippedHostDrawsTextFlippedSoItShowsUpright` проверял само предположение (краска внизу картинки), поэтому прошёл. | Скриншот демо `Demo` на Mac от пользователя. | исправлен `df51cd5` |
| 130 | 2026-09-24 | `NodesUIKit/NodesAppKit` `intrinsicContentSize` | `NodeView`/`NodeNSView` сообщали шириной собственного размера max-content дерева — ширину самой длинной строки без переносов. SwiftUI брал её за идеальную ширину и растягивал экран шире телефона: содержимое обрезалось с двух сторон, `Breakpoint` видел широкую карточку. | Скриншот iOS-демо (`DemoiOS.swiftpm`) на симуляторе iPhone 18 Pro от пользователя. | исправлен `ce44b1f` |
| 131 | 2026-09-24 | `NodesRender` `LayerRenderer` (анимации) | После анимированной отрисовки на слоях нет части добавленных анимаций: `animation(forKey:)` возвращает `nil` для сдвига кадра с кривой по умолчанию, цвета фона и угасания ушедшей ноды; пружина на сдвиге, проявление новой ноды и угасание скрытой — на месте. | `swift test` на Mac: 4 из 10 тестов `AnimationTests` падают, при повторе — 8 из 10. Трассировка рендерера: все анимации добавлены и есть на слоях в конце отрисовки, их снимает `commit` транзакции — у слоёв вне окна. Внутри открытой внешней транзакции анимации на месте; на экране (демо на iOS Simulator) анимации видны. Дефект теста, не рендерера: тесты проверяли слои после `commit`. Почему голые слои в пробе анимации при `commit` сохраняли, не установлено. | исправлен `34a24e2` — тесты держат транзакцию открытой до конца теста |
| 132 | 2026-09-24 | `NodesAppKit` `NodeAccessibilityElement.accessibilityPerformPress` | Метод AppKit объявлен без изоляции, а элемент — `@MainActor`: обращение к `host` и `NodeHost.activate` из неизолированного метода. Предупреждение компилятора, в строгом режиме Swift 6 — ошибка. | Предупреждение при `swift run` демо на Mac. | первое исправление `d874474` (`nonisolated` + `MainActor.assumeIsolated`, замыкание захватывало `self`) не собиралось в Xcode: «sending 'self' risks causing data races». Исправлен `6aa3165` — нажатие хранится как `let` с `@MainActor @Sendable` замыканием (слабый хост, `NodeID`), `self` в неизолированный метод не попадает |
| 133 | 2026-09-24 | `NodesUIKit` `NodeFocusItem.focusItemContainer` | Фокус-элемент ноды отдавал `NodeView` как свой `focusItemContainer`. Это контейнер детей самого элемента, а не соседей: движок фокуса находил в нём все элементы дерева как детей сфокусированного, и стрелки пульта не двигали фокус. Начальный фокус и Select работали. | Живой запуск `DemotvOS` на Apple TV Simulator (tvOS 27): фокус стоит на Rename Ada, стрелки не работают. | исправлен `928cd86` — `focusItemContainer` = `nil`: нода фокусируется целиком, детей у элемента нет |
| 134 | 2026-09-24 | `NodesUIKit` фокус tvOS | После исправления #133 стрелки пульта по-прежнему не двигают фокус: начальный фокус на Rename Ada, Select работает. | Живой запуск `DemotvOS` на Apple TV Simulator (tvOS 27). Временный лог `os.Logger` в `NodeView` показал: элементы и кадры верны, стрелки доходят, движок фокуса спрашивает `focusItems(in:)` в полосе шириной с фокусированную кнопку по направлению нажатия — и находит в ней только её саму: бейджи стоят в другом столбце. Так ищет сама tvOS; дефект раскладки демо, не библиотеки. | исправлен `13c5773` — в демо кнопка Rename Ada прижата к правому краю, под столбец бейджей; лог убран |
| 135 | 2026-09-24 | `NodesUIKit` `NodeFocusItem.focusGroupIdentifier` | Для групп фокуса на iPad (секция = группа) фокус-элемент объявлял `focusGroupIdentifier`; на tvOS это свойство недоступно — сборка tvOS падала: «Cannot override 'focusGroupIdentifier' which has been marked unavailable». Ветку по платформе можно сделать только `#if os`, а он запрещён. | Сборка `DemotvOS` в Xcode. | исправлен `c4bc01b` — группы фокуса убраны; на iPad секции — только направляющие, как на tvOS |
| 136 | 2026-09-25 | `Benchmarks` `LayoutBench.cancelLatency` | Задержку отмены замер запускал в `Task.detached` — на потоке кооперативного пула. На Apple у него стек 512 КиБ, и `deep-1500` (1500 уровней) переполняет его ещё до расчёта, в рекурсивном `Solver.flatten` (разворот входного дерева; рекурсия не только в шагах солвера, см. #124): SIGBUS, у обоих движков. На Linux у потоков пула 8 МиБ, поэтому замеры E3 проходили. Кроме того, замер не совпадал с продуктом: `NodeHost` решает в фоне на своём потоке со стеком 8 МиБ. | `swift run -c release LayoutBench` на Mac (macOS 27, arm64): `bus error` после `deep-30`; lldb — `EXC_BAD_ACCESS` в потоке `Task 2` (`com.apple.root.default-qos.cooperative`), стек `Solver.flatten`, главный поток ждёт в run loop (`time()` на главном прошёл). | исправлен `424c8f8` — расчёт при замере отмены на своём потоке со стеком 8 МиБ |
| 137 | 2026-09-25 | `LayoutCore` DSL `padding(top:leading:bottom:trailing:)` | На элементе модификатор всегда оборачивал его в бокс-вставку, даже когда все стороны `nil` («`nil` оставляет значение как есть»). Обёртка меняет раскладку: модификаторы, написанные до `padding`, уходят внутрь бокса, а элемент растягивается в нём — `badge.alignSelf(.center).padding(top: none)` в строке занимает всю высоту вместо центра. | Тест `paddingWithEverySideNilChangesNothing` при работе над опциональными аргументами модификаторов. | исправлен `34fac5e` — без единой стороны `padding` возвращает спеку как есть |
| 138 | 2026-09-25 | `Nodes` `Node.setNeedsLayout` при фоновом расчёте | Хост ноды ищется подъёмом по `supernode`, а нода, которую фоновый проход в полёте только смонтирует, ещё ни к чему не подвешена: её `setNeedsLayout` терялся. Изменение содержимого такой ноды во время расчёта не запускало нового прохода, и после применения она показывала устаревший размер — до следующего постороннего прохода. | Тест `aDeepTreeIsSolvedOnAThreadWithRoomForIt` после #124: первый проход глубокого дерева ушёл с главного потока, и изменение листа до конца расчёта пропало. | исправлен `2b9e8aa` — ноды прохода в полёте знают хост (`pendingHost`) до применения или отмены |
| 139 | 2026-09-25 | `LayoutCore` DSL `LayoutTree.nodes` (сборка спеки) | Перевод спеки в дерево движка рекурсивен по уровню вложенности (`nodes(for:)` → `embeddedLayout` → `layoutSpec()` вложенной ноды) и идёт только на главном потоке — он вызывает `layoutSpec()` нод. На главном потоке 1 МиБ цепочка вложенных нод падает на 67 в debug и 275 в release — раньше, чем солвер (71 / 411 уровней), так что для экранов на нодах предел глубины задаёт сборка, а не расчёт. | Проба с главным потоком 1 МиБ (`-Xlinker -stack_size 0x100000`) после #124: `NodeHost` с бюджетом по умолчанию, `EXC_BAD_ACCESS` в `LayoutSpec.init(element:)` ← `Nest.layoutSpec()` ← `Node.embeddedLayout`. | исправлен `5876821` — `LayoutTree` обходит спеку явным стеком; до 2000 нод на главном 1 МиБ без падений |
| 140 | 2026-09-25 | `LayoutCore` `LayoutNode` (освобождение дерева) | Дерево движка — вложенные массивы значений; рантайм Swift освобождает его рекурсивно (`swift_arrayDestroy` → `_ContiguousArrayStorage` deinit на каждый уровень). На главном потоке 1 МиБ цепочка из ~3810 уровней падает при освобождении — и в debug, и в release, уже после того как проход отклонён или применён. Далеко за реальными деревьями: движок на потоке 8 МиБ отклоняет дерево глубже ~580 уровней в debug. | Проба с главным потоком 1 МиБ после #139: 3804 уровня — выход 0, 3816 — `EXC_BAD_ACCESS` в `swift_arrayDestroy`. | исправлен `516072b` — `PreparedLayout` освобождает дерево по уровню за раз; копию у потока солвера освобождает её последний владелец — проба до 8000 уровней без падений |
| 141 | 2026-09-25 | `NodesRender` `LayerRenderer.sync` | Сверка слоёв с деревом нод рекурсивна по уровню вложенности и идёт на главном потоке. На главном потоке 1 МиБ отрисовка цепочки нод падает на 109 уровнях в debug и 376 в release — раскладка той же цепочки после #139 проходит до 2000. | Проба с главным потоком 1 МиБ: раскладка, затем `LayerRenderer.render`; `EXC_BAD_ACCESS` в `-[CALayer setBounds:]` ← `LayerRenderer.sync` × N. | исправлен `ff433cd` — сверка слоёв явным стеком; регрессионного теста нет — на Mac главный поток 8 МиБ, проверено пробой с 1 МиБ |
| 142 | 2026-09-25 | `Nodes` `NodeHost.collect` / `collectFocus` / `collectSections`, `Node.hitTest` | Сбор элементов доступности, фокуса и поиск ноды под точкой рекурсивны и идут на главном потоке. На главном потоке 1 МиБ release падает в `accessibilityItems` на ~2910 уровнях, хотя движок на своём потоке принимает дерево до ~3270. В debug недостижимо: движок отклоняет дерево глубже ~520, а обходы проходят до этого предела. | Проба отрисовки с главным потоком 1 МиБ после #141: 2909 — все шаги, 2913 — падение на шаге `accessibility`. | исправлен `516072b` — обходы явным стеком (`Node.walkVisible`, `hitTest`) |
| 143 | 2026-09-25 | `LayoutCore` `FlexItem.mainIsDefinite` | В column без заданной высоты высота элемента после flex считалась неопределённой, и проценты высоты его детей вели себя как `auto`. Chromium считает её определённой, если у элемента `flex-basis` — длина (`0`, `5px`), а не `auto`, неразрешимый процент или `content`: `height: 50%` внука в элементе `flex-basis: 0` высотой 10 даёт 5, движок давал 10. | Разбор #118: `random/0013`, уменьшенный до 4 нод автоматическим уменьшителем; эталон — Chromium 152 во встроенном браузере (на наборе совпадает с записанным Chromium 141 в 910 из 912 кейсов). | исправлен `90631ed` |
| 144 | 2026-09-25 | `LayoutCore` `ratioLayout` (контейнер с `aspect-ratio`) | Размер контейнера с пропорцией по зависимой стороне брался из пропорции как есть. Chromium (CSS Sizing 4 §5.2.1) не опускает его ниже содержимого — как движок уже делал для листьев: (а) высота из ширины при `min-height: auto` поднимается до высоты содержимого, но содержимое меряется при высоте из пропорции — stretch-дети берут её, поднять могут только дети с заданной высотой; (б) размер содержимого по оси (замер базы и автоматического минимума flex-элемента) — max(перенесённый через пропорцию, содержимое): `height: 10; aspect-ratio: 1` с содержимым шириной 50 — 50, движок давал 10; `width: 120; aspect-ratio: 1` в column с `max-height: 60` не сжимается ниже 120. | Разбор #118: `random/0020`, `0033`, `0038`, `0082`, `0120`, `0154`, `0188`, `random-text/0110`, уменьшенные до 2–4 нод; эталон — Chromium 152 во встроенном браузере. | исправлен `90631ed`, `3040bd8` |
| 145 | 2026-09-25 | `LayoutCore` column с `wrap` (линии и собственная ширина) | Исправление #116 угадало половину. Column без определённой высоты рвал линии по доступной высоте, а ширину при неизвестной ширине считал по самому широкому элементу. В Chromium column рвёт линии только по своей высоте, иначе по `max-height`, иначе это одна линия; max-content ширина — линии рядом, min-content — самый широкий элемент; в определённом месте (absolute) — fit-content между ними. `height: 80; flex-wrap: wrap` с элементами 40×80 и 90×10 — 130 в ширину, движок давал 90. | Разбор #118: `random/0029`, `0115`, `0008` (кейс #116), `random-baseline/0051`; пробы во встроенном браузере (Chromium 152). | исправлен `90631ed`, `3040bd8` |
| 146 | 2026-09-25 | `LayoutCore` `layoutAbsoluteChildren` | Absolute-элемент с `top` и `bottom` всегда растягивался между ними, а при заданной высоте его ставил `top`. В Chromium (CSS Align §5.1 для absolute) `align-self`, отличный от `auto`/`stretch`, даёт высоту по содержимому и выравнивание в области между вставками — всегда по вертикали, при любом направлении flex, `flex-start` сверху даже в `wrap-reverse`; `align-items` родителя не участвует. `top: 10; bottom: 10; align-self: flex-end` с содержимым 20 — y 170, движок давал y 10 и высоту 180. | Разбор #118: `random/0075`, `0210`, `random-text/0035`, `0113`, уменьшенные до одной ноды; пробы во встроенном браузере (Chromium 152). | исправлен `90631ed` |
| 147 | 2026-09-25 | `LayoutCore` базовая линия (`placeLines`, `containerBaseline`, статическая позиция absolute) | (а) Расстояния от базовой линии до краёв строки начинались с нуля, хотя базовая линия может лежать вне элемента (содержимое вылезает из меньшего бокса): строка выходила выше, элемент сдвигался. (б) В `row-reverse`/`column-reverse` базовую линию контейнеру давал первый по порядку элемент; в Chromium — физически первый, с конца строки. (в) Absolute без вставок с `baseline` в `wrap-reverse` вставал к низу; baseline без общей линии — это начало письма, которое `wrap-reverse` не переворачивает. | Разбор #118: `random-baseline/0069` (записан в E2 как «не разобран»), `0073`, `0062`, `0075`, `0016`; пробы во встроенном браузере (Chromium 152). | исправлен `3040bd8` |
| 148 | 2026-09-25 | `LayoutCore` `flexLayout` (ширина по содержимому и проценты детей) | Контейнер, чья ширина идёт от содержимого (absolute без ширины, элемент column без stretch), мерял детей, не зная своей ширины: процентная ширина ребёнка оставалась `auto` и при раскладке, текст не переносился, высота была одной строкой. В CSS ширина блока считается до детей: пока она от содержимого, процент циклический, затем разрешается от неё, и ребёнок раскладывается по ней. `max-width: 50%` у текста в absolute: Chromium — 80×60, движок — 80×20. | Разбор #118: `random-text/0057`, `0189`, `random-baseline/0033`, `0022`, уменьшенные до 2–3 нод; эталон — Chromium 152 во встроенном браузере. | исправлен `3040bd8` |
| 149 | 2026-09-25 | `LayoutCore` `layoutAbsoluteChildren` (absolute с `aspect-ratio`) | Absolute с пропорцией переносил сторону через пропорцию сам, без автоматического минимума, а высоту растягивал между `top` и `bottom`. Chromium сначала решает ширину: заданная (при `min-width: auto` не меньше min-content — только у absolute, у элементов потока так нет), между вставками или из высоты через пропорцию; высота затем идёт из ширины через пропорцию и поднимается до содержимого — вставки её не растягивают. `left: 5; right: 0; top: 20; bottom: 20; aspect-ratio: 1` — 355×355, движок давал 355×260; `width: 20; height: 80; aspect-ratio: 1` с содержимым 30 — ширина 30. | Разбор #118: `random/0284`, `0305`, `0394`, `random-text/0036`, `0052`, `0054`, `0066`, `random-baseline/0018`, `0027`; пробы во встроенном браузере (Chromium 152). | исправлен `3040bd8` |
| 150 | 2026-09-25 | `LayoutCore` `layoutAbsoluteChildren` (место и сдвиг) | (а) Absolute с явным `align-self` между `top` и `bottom`, вылезающий за containing block, оставался там, куда его поставило выравнивание; Chromium сдвигает его внутрь, верхний край важнее (при `auto` — старое правило CSS2, `top` побеждает). (б) Без горизонтальных вставок доступная ширина считалась от всего containing block; бокс стоит в статической позиции, и padding родителя с начальной стороны ему не достаётся. | Разбор #118: `random/0210`, `random-baseline/0029`, `random-text/0185`; пробы во встроенном браузере (Chromium 152). | исправлен `3040bd8` |
| 151 | 2026-09-25 | `LayoutCore` `flexLayout` (row с `wrap` без ширины) | Row с переносом, чья ширина идёт от содержимого (элемент column без stretch, с `max-width`), рвал линии по предложенному месту, а не по своей ширине: fit-content, ограниченной `min/max-width`. `max-width: 40` с элементами 50 и 0 — Chromium две линии, движок одна. Исправлено тем же путём, что #148: ширина — до раскладки. | Разбор #118: `random/0355`, `0360`, `random-text/0102`, уменьшенные до 2–4 нод; эталон — Chromium 152 во встроенном браузере. | исправлен `3040bd8` |
| 152 | 2026-09-25 | `LayoutCore` автоматический минимум по пропорции | (а) Высота из пропорции поднималась до содержимого и при процентной высоте, которая ведёт себя как `auto`; Chromium поднимает только при `height: auto` (для ширины так не делает). (б) В column, чья ширина считается по содержимому, вклад элемента с пропорцией и заданной высотой шёл от высоты после flex, а не от заданной. | Разбор #118: `random/0300`, `0267`, `random-baseline/0116`; пробы во встроенном браузере (Chromium 152). | исправлен `3040bd8` |
| 153 | 2026-09-25 | `LayoutAppKit`, `NodesAppKit` (сборка для Mac Catalyst) | Адаптеры AppKit закрыты `#if canImport(AppKit)`, а в Mac Catalyst AppKit импортируется, хотя `NSView` недоступен: пакет не собирается для Catalyst (`'NSView' is unavailable in Mac Catalyst`), и тесты UIKit-адаптеров на Catalyst не запускаются. | Первый прогон `xcodebuild test -destination 'platform=macOS,variant=Mac Catalyst'` (проверка фокуса в Catalyst из «не сделано» 03). | исправлен `c4eb90e` (решение 2026-09-25: `#if canImport(AppKit) && !canImport(UIKit)` — без ветвления по платформе; в Catalyst работает UIKit-адаптер; тесты проходят на Catalyst) |
| 154 | 2026-09-25 | `NodesRender.Image` | Смена `contentMode` просила render, но не меняла `drawingRevision`; `LayerRenderer` пропускал перерисовку при том же размере. | Ревью нового `Image` после полного тестового прогона: сравнение `contentMode.didSet` с ключом `LayerRenderer.Drawing`. | исправлен `da0d829`: ревизия растёт при смене режима, `changingContentModeRedrawsTheSameFrame` проверяет пиксели |
| 155 | 2026-09-25 | `NodesRender.ImagePipeline.load` | Документ и комментарий обещают, что разные размеры декодируются последовательно внутри pipeline. На деле `Task { try Self.decode(…) }` не захватывает `self` и не наследует изоляцию актора: декодирования идут параллельно на общем пуле, несколько полноразмерных bitmap создаются одновременно. | Ревью `Image`: проба той же конструкции (`actor` + `Task { Self.nonisolatedStaticWork() }`, Swift 6) — 4 задачи по 0,5 с завершились за 0,5 с, а не за 2 с. | исправлен `065d5df`: `DecodeGate` пропускает одно декодирование за раз вне актора, отменённый в очереди запрос уходит без работы; `differentImagesDecodeOneAtATime` (без очереди пик — 6), `cancelledQueuedDecodeLeavesTheQueue` |
| 156 | 2026-09-25 | `NodesRender.ImageCache.prepare` | При `metadata` ≠ `.preserve` формат, который Image I/O читает, но не пишет (WebP), даёт `processingFailed` из `CGImageDestinationCreateWithData`; `load` бросает, и картинка не показывается вовсе, хотя декодируется. Настройка приватности кэша ломает показ. | Ревью `Image`: `CGImageDestinationCopyTypeIdentifiers` на macOS 27 — WebP читается, но не пишется. | исправлен `b69317c`: `ImageCache.load` показывает такую картинку без записи на диск (оригинал сохранил бы то, что политика удаляет); `imageThatCannotTakeTheMetadataPolicyIsShownButNotCached` — WebP через подменённый HTTP-ответ |
| 157 | 2026-09-25 | `NodesRender.Image.layoutContent`, `LayoutCore` | Картинка отдавала движку фиксированный размер в пикселях (`LeafContent.size`), без пропорции: при ширине от контейнера или явной `width` высота оставалась исходной. Картинка 400×200 в столбце шириной 100 — рамка 100×200, Chromium для `<img>` — 100×50; `.fit` показывал полосы, `.fill` обрезал. Масштаба источника нет: ресурс @2x/@3x занимал вдвое/втрое больше точек. | Ревью `Image`; пробы движка через `NodeHost` и `<img>` 400×200 во встроенном браузере (Chromium 152), 7 случаев. | частично исправлен `fa542ed`: высота по ширине (`NaturalSizeMeasurer`) и `scale`, `imageKeepsItsProportionsForTheWidthItGets`, `scaleTurnsSourcePixelsIntoPoints`, `placeholderSizeKeepsItsProportionsToo`. Ширина по высоте исправлена `05e71d4`: `LeafContent.proportional` — собственная пропорция как `aspect-ratio: auto`, сторона по пропорции без минимума по содержимому; `imageTakesItsWidthFromItsHeight`, эталоны `fixtures/image.json` (Chromium 153) — 28 из 29, без подстановки пропорции или с минимумом расходятся 7; остаток — отступы, #169 |
| 158 | 2026-09-25 | `NodesRender.ImageCache.load` | Одновременные запросы одного URL скачивали его столько раз, сколько было запросов: актор отпускается на `await` сети, второй вызов не находит файла и начинает свою загрузку. Один аватар в 20 ячейках — 20 запросов и 20 записей на диск. Объединялись только декодирования. | Ревью `Image`: чтение `ImageCache.load` (проверка диска → `await URLSession` → запись). | исправлен `08d4ff9`: общая загрузка на URL, последний ушедший отменяет её до записи; `simultaneousLoadsOfOneURLShareOneDownload` (до исправления — 3 запроса), `aCancelledLoadLeavesTheSharedDownloadToTheOthers` |
| 159 | 2026-09-25 | `NodesRender.ImageCache` (сеть) | Загрузка шла только через `URLSession.shared` и целиком в память (`data(from:)`) без предела: ответ любого размера держался в памяти, прежде чем проверялся как картинка; сессию (заголовки, таймауты, авторизация) не передать, и тесты подменяли сеть только глобальной регистрацией `URLProtocol`. | Ревью `Image`: чтение `ImageCache.load`. | исправлен `da64b69`: `ImageCache(configuration:session:)`, `maximumDownloadBytes` обрывает ответ по счётчикам байтов задачи; `responseOverTheDownloadLimitIsRejectedAndNotCached` (с `Content-Length` и без), `errorResponsesAreNotCached`; тесты без глобального `URLProtocol` |
| 160 | 2026-09-25 | `NodesRender.ImagePipeline.load` | Попадание в кэш декодированных картинок не избавляло от диска: каждый `load` с URL сначала читал файл (или запись дискового кэша) целиком и считал SHA-256 от всех байтов, и только потом смотрел в кэш памяти. Прокрутка списка с уже показанными картинками читала и хэшировала их заново на каждом запросе размера. | Ревью `Image`: порядок в `ImagePipeline.load` (`cache.load` → `SHA256` → `decoded[key]`). | исправлен `76e7a5e`: отпечаток по штампу файла, кэш памяти до чтения; `unchangedFileIsNotReadAgainForADecodedSize`, `cachedRemoteImageIsNotReadAgainForADecodedSize`. По ходу `replacingFileBytesDoesNotReuseAnOldBitmap` поймал в первом варианте кэш `URL.resourceValues` (старый штамп после замены файла) — атрибуты читаются через `FileManager` |
| 161 | 2026-09-25 | `NodesRender.ImageCache` (диск) | (а) Вытеснение считало и удаляло **любые** файлы папки: при `directory` вроде корня `Caches` кэш удалял чужие файлы. (б) Очистки нет — ни всего кэша (выход из аккаунта), ни одного URL. (в) Просроченные файлы удалялись только при чтении. (г) Каждая запись сканировала всю папку с атрибутами каждого файла. | Ревью `Image`: чтение `ImageCache.store`/`evictIfNeeded`. | исправлен `8b9e5fc`: только файлы с именем записи; `removeAll`, `remove(for:)`, `removeExpired`; счётчик размера (пересчёт при лимите и раз в 64 записи); загрузка после `removeAll` не пишется. Тесты: `evictionAndRemovalLeaveOtherFilesInTheDirectory`, `removingOneURLKeepsTheOthers`, `expiredEntriesAreRemovedWithoutBeingRead`, `writesDoNotScanTheDirectoryEachTime`, `downloadFinishingAfterRemoveAllIsNotStored` — каждый падает при отключении своей части |
| 162 | 2026-09-25 | `NodesRender.ImageCache` (общая папка) | Все кэши по умолчанию пишут в одну папку (`Caches/image-cache`), и каждый считал все записи папки против **своего** `maximumBytes`: кэш с меньшим лимитом вытеснял записи кэша с другими политиками и большим лимитом (например, кэш без GPS с 10 МБ — записи `ImagePipeline.shared` с 256 МБ). По имени файла нельзя было понять, какими политиками он записан. | Ревью `Image`: `evictIfNeeded`/`removeExpired` при общей `directory` по умолчанию. | исправлен `0474419`: имя записи — метка политик + отпечаток URL, лимит и срок по своей метке, `removeAll` — все политики, прежние имена удаляются при пересчёте; `cachesSharingADirectoryKeepTheirOwnLimits` (падает, если считать чужие записи своими), `filesOfTheEarlierNamingAreRemovedOnRecount` |
| 163 | 2026-09-25 | `NodesRender.Image`, `Nodes.Node` (монтирование) | Нода, ушедшая из дерева (`if` в `layoutSpec`, сторона `Breakpoint`), но живая в свойстве экрана, продолжала загрузку и держала декодированный bitmap: отмена была только в `deinit` и при смене источника. Открытого события монтирования у `Node` нет — `mount`/`unmount` внутренние и `mount` вызывается на каждом проходе. | Ревью `Image`: чтение `Image.deinit`/`reload` и `Node.mount`/`unmount`. | исправлен `8ae7764`: `Node.mountedChanged(_:)` на переходах; `Image` при уходе отменяет загрузки и отпускает bitmap, при возвращении возобновляет; `mountedChangedRunsOnJoiningAndLeavingOnly`, `imageLeavingTheTreeReleasesPixelsAndGetsThemBack`, `imageLeavingTheTreeCancelsItsDownloadAndResumesOnReturn` (оба падают без реакции на уход). Приоритет видимых — вне этого дефекта: в `Scroll` нет виртуализации |
| 164 | 2026-09-25 | `NodesRender.ImageCache.download` (исправление #161) | Загрузка запоминала счётчик `removeAll()` в начале тела своей задачи, а задача могла начаться на акторе уже после `removeAll()`: запись в `downloads` видна сразу при создании. Тогда загрузка, начатая до `removeAll()`, писала результат на диск. | `downloadFinishingAfterRemoveAllIsNotStored` стал падать при запуске отдельно (3 из 3) — в общем прогоне проходил за счёт тайминга; разбор при работе над #163. | исправлен `6bc98f2`: счётчик берётся в `load` при создании загрузки; `downloadFinishingAfterRemoveAllIsNotStored` отдельно — 5 из 5 |
| 165 | 2026-09-25 | `DemoiOS.swiftpm` (сборка) | `xcodebuild` не собирает iOS-демо: корневой пакет и зависимость `../Demo` оба называются `LayoutDemo` (`unable to override package 'LayoutDemo' because its identity 'demo' doesn't match override's identity 'demoios.swiftpm'`), а у цели `AppModule` не указаны зависимости, хотя `App.swift` импортирует `DemoScreens` и `NodesUIKit`. | Проверка раздела картинок демо в iOS Simulator: сборка из командной строки. Копия с другим именем пакета и явными зависимостями собралась и запустилась. Сборка из Xcode не проверялась. | исправлен `a1007c6`: корневой пакет — `LayoutDemoiOS` (приложение по-прежнему `LayoutDemo`), у цели `AppModule` — зависимости `DemoScreens` и `NodesUIKit`. `xcodebuild build -scheme LayoutDemoiOS` для iPhone 17 Simulator собирается, приложение запускается и показывает экран. Из Xcode (GUI) и Swift Playgrounds — не проверялось; манифест помечен генерируемым, и Xcode при правке настроек приложения может его переписать |
| 166 | 2026-09-25 | `NodesRender.ImageCache.prepare` (политика метаданных) | `CGImageDestinationCopyImageSource` вызывался только с флагами исключения, без `kCGImageDestinationMetadata`: Image I/O тогда заменяет метаданные пустыми. `.removeLocation` удалял не только GPS, а весь EXIF и XMP — в том числе **ориентацию**: повёрнутое фото с камеры после кэша с этой политикой показывалось боком. Документ обещал, что ориентация и профиль сохраняются; профиль сохранялся. `.removeLocation` и `.removeLocationAndXMP` вели себя одинаково. | Тест `removeLocationAndXMP` проходил и без флага XMP; проба: JPEG с ориентацией 6, датой EXIF и P3 — после копии с `ExcludeGPS` ориентации и даты нет, все теги XMP удалены. | исправлен `2a8352b`: метаданные источника передаются без слияния с исключением GPS; для `.removeLocationAndXMP` — только теги EXIF/TIFF (флаг XMP при явных метаданных не действует). `metadataPoliciesKeepOrientationAndPixels` — обе политики падают на старом коде, фото показывается боком |
| 167 | 2026-09-25 | `removingLocationMetadataPreservesEncodedImagePixels` (тест) | Исходный JPEG теста строился со словарём GPS из одной широты; Image I/O такой неполный словарь не записывает, и GPS в исходнике не было — проверка «после обработки GPS нет» ничего не проверяла. | Разбор #166: проба с тем же словарём показала `gps: false` уже у исходника. | исправлен `2a8352b`: `cameraJPEG()` с полным GPS, тест сначала проверяет, что у исходника всё есть |
| 168 | 2026-09-25 | `NodesUIKit` фокус tvOS (прокрутка) | На Apple TV фокус не переходит вниз к кнопке, если между ней и последней фокусируемой нодой стоит блок без фокусируемых нод: в демо — раздел картинок (~165 pt раскладки, ×2 на TV) между последней карточкой и «Rename Ada». Прокрутка останавливается на последнем бейдже, кнопка остаётся под краем экрана (y = 1229 при экране 1080), «вниз» больше ничего не делает. Без раздела тот же тест проходит. С разделом только из placeholder (без загрузки) и при ожидании 5 с перед нажатиями — тоже падает: дело не в загрузке и не в `Image`-специфике раскладки. `NodeHost.focusItems` ноды по видимости не отсекает, масштаб `factor` в `ScrollFocusContainer` учтён последовательно. Пустая нода той же высоты вместо раздела (без `Image`) — тоже падает; раздел после кнопки — проходит: дело в нефокусируемом блоке, не в картинках. Вероятная причина (не проверена): на TV бейдж справа, кнопка слева — не в полосе поиска под бейджем; вести к ней должна направляющая секции `Actions` (`isFocusSection`), но `SectionGuide` ставится в `NodeView` по положению секции на экране, и секция прокрутки, целиком ушедшая за край, для UIKit недоступна. | UI-тест `FocusScrollTests` (tvOS Simulator, Apple TV 4K 3rd gen) после добавления раздела картинок в демо; эксперименты в отдельных worktree, журнал рамки кнопки после каждого нажатия. | открыт, отложен (2026-09-26, решение пользователя). Разбор по журналу `NSLog` на Simulator: причина подтверждена — UIKit опрашивает `ScrollFocusContainer.focusItems(in:)` узкой полосой под бейджем (x 1566–1680, до ~1000 pt вглубь содержимого), а `NodeView` — той же полосой в пределах экрана (до y ≈ 1022 при высоте 960); направляющая `Actions` стоит на y = 1164, кнопка — в x 48–268, вне полосы. Не сработало: (1) контейнер возвращает цель секции, если полоса задевает рамку секции, — UIKit получает её, но не выбирает (рамка цели вне полосы); (2) прокси секции во всю ширину (`SectionEntry`) — UIKit выбирает его, прокси отклоняет фокус и просит у хоста кнопку, запрос доходит до `requestFocusUpdate`, но программный перевод на элемент за экраном UIKit не выполняет (`didUpdateFocus` не приходит). Дальше: прокрутить контейнер к секции до запроса или вести секции прокрутки через настоящий `UIScrollView` с `UIFocusGuide` в содержимом. Регрессионный тест готов: раздел картинок перед кнопкой в демо + `FocusScrollTests`. Демо пока обходит: раздел после кнопки |
| 169 | 2026-09-26 | `LayoutCore` пропорциональное содержимое с отступами | Собственная пропорция картинки (`LeafContent.proportional`) применяется ко всей рамке, как `aspect-ratio` при `box-sizing: border-box`; у `<img>` она относится к области содержимого. Картинка 400×200 с `padding: 10` в столбце шириной 120: Chromium 120×70 (100/2 + 20), движок 120×60. Без отступов расхождения нет. | Эталоны `fixtures/image.json`, кейс `image/padding`. | исправлен `027cc16`: `BoxRatio` — собственная пропорция по области содержимого во всех переносах (размер листа, собственные размеры, раскладка с пропорцией, флекс-база); `image/padding` и 12 новых кейсов с отступами совпадают с Chromium, поломка переноса роняет 12 из них |
| 170 | 2026-09-26 | `Benchmarks` (зависимости) | Пакет бенчмарков не собирается: старый движок для сравнения — `resoul/test` на ревизии `2d20d678`, а её манифест зависит от `https://github.com/resoul/flux.git`, которого больше нет (библиотека переименована в AsyncRay). SwiftPM разрешает граф целиком, поэтому не работает и `--engine current`. | Проверка скорости правки #157: `swift run -c release LayoutBench` на `main` и на ветке — `Repository not found`. Скорость сравнена копией бенчмарка без старого движка (`--engine current`), по два прогона: расхождения в пределах шума. | исправлен `df3cee4`: исходники старого движка на той же ревизии — замороженная локальная цель `PreviousLayoutEngine`, пакет ничего не берёт извне `v22/` (решение пользователя 2026-09-26: старую библиотеку не трогаем, нужное переносим в `v22`); `swift run -c release LayoutBench` снова сравнивает оба движка |
| 171 | 2026-09-26 | `LayoutCore` абсолютное позиционирование пропорционального содержимого | Абсолютно позиционированная картинка (`LeafContent.proportional`) без своего размера по оси растягивается между двумя отступами этой оси, как блок, и её пропорция переносит размер на другую ось. У `<img>` в Chromium отступы размер не задают: картинка 400×200 с `top: 10; bottom: 10` в контейнере 600×300 — Chromium 400×200, движок 560×280; 40×20 с `left: 10; right: 10` — Chromium 40×20, движок 580×290. | Новые эталоны `fixtures/image.json` (`image/absolute-insets`, `image/absolute-horizontal-insets`, `image/padding-absolute-insets`) при исправлении #169. | исправлен `027cc16`: `AbsoluteLayout` не растягивает пропорциональное содержимое между отступами; 3 кейса совпадают с Chromium, без исправления падают |
